services:
  ai-orchestrator:
    build:
      context: ../ai-orchestrator
      dockerfile: Dockerfile
    ports:
      - "8088:8080"
    volumes:
      - ../config:/config:ro
    environment:
      - BOOKLORE_BASE_URL=http://booklore:8080
      - MODELS_CONFIG_PATH=/config/models.yml

